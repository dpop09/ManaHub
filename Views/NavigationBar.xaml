<UserControl x:Class="ManaHub.Views.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ManaHub.Views"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="1280">
    <Border Background="#F0F0F0" BorderBrush="Black" BorderThickness="0,0,0,1">
        <Grid Margin="5,0">
            <TextBlock Text="{Binding NavVM.Username}" VerticalAlignment="Center" FontWeight="Bold" HorizontalAlignment="Left"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Width="100" Height="22" Content="Logout" 
                        Command="{Binding NavVM.ExecuteLogoutCommand}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
